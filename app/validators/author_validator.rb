class AuthorValidator < ActiveModel::Validator

    # def validate(record)
    #     unless record.author.match?(@name, )
    #         record.errors[:post] << "This post is invalid" 
    #     end
    # end
end